# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# prisma generated client (regenerated on install + in the Docker build)
apps/api/src/generated/

# tests
coverage/

# secrets — NEVER commit real env; .env.example documents the keys with empty values
.env
.env.local
.env.*.local

# openapi export artifact
openapi.json

# logs / os
*.log
.DS_Store
